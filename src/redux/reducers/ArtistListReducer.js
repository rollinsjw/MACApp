import { }

const INITIAL_STATE = {
  artists: {
    john: {
      medium : 'oil'
      location: 'georgia'
    }
  }
}

export default (state = INITIAL, action_ =>{
  switch(action.type) {
    default:
      return state;
  }
})
